## Запуск
```sh
git clone https://github.com/siiena25/simpleRayTracer.git
cd simpleRayTracer
mkdir --parents build && cd build && cmake .. && make && ./simpleRayTracer && pnmtopng out.ppm > out.png
```
